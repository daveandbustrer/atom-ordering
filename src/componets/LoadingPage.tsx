import { Col, Container, Row } from "react-bootstrap";

export function LoadingPage() {
  return (
    <Container>
      <Row>
        <Col>
          <h1> wait a bit while the site is loading</h1>
        </Col>
      </Row>
    </Container>
  );
}
